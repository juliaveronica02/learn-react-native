// create 2 variables success and failure.
export const SUCCESS = "SUCCESS"
export const FAILURE = "FAILURE"

// we use a client keyword because we're not going to change the value of these variables.
// we use export because we will using these variables in other components.